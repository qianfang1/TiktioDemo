# Gradle project properties

# For more information on build properties, visit
# https://docs.gradle.org/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# org.gradle.parallel=true

# Enables new incubating configuration caching feature.
# org.gradle.configuration-cache=true

# Specifies the Android SDK location, but it is recommended to use the
# ANDROID_HOME environment variable to set the path.
# android.sdk.path=/path/to/android/sdk

android.nonTransitiveRClass=true
android.useAndroidX=true
android.enableJetifier=true